# Nexuscone funding targets — read by GitHub to render the Sponsor button.
# Only approved accounts render; listing both the personal and org handles
# means whichever is enrolled first lights up.
github: [osiabu, nexuscone]
custom: ["https://aperintel.com"]
